<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			 xmlns:local="using:ImageProcessing.SurveyImageSmooth.Views"
			 xmlns:vm="using:ImageProcessing.SurveyImageSmooth.ViewModels"
             mc:Ignorable="d" d:DesignWidth="400" d:DesignHeight="350"
             x:Class="ImageProcessing.SurveyImageSmooth.Views.ImageViewer"
			x:DataType="vm:ImageSetModel">
	<DockPanel>
		<Panel DockPanel.Dock="Top">
			<Button>👀</Button>
		</Panel>
		<ListBox DockPanel.Dock="Top" ItemsSource="{Binding Images}">
			<ListBox.ItemTemplate>
				<DataTemplate>
					<Image Margin="2" Height="200" Width="200" Source="{Binding ImageSource}"/>
				</DataTemplate>
			</ListBox.ItemTemplate>
		</ListBox>
		<local:ImagePresenter />
	</DockPanel>
</UserControl>
